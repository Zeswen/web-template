{
  "name": "@zeswen/db",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev --name"
  },
  "dependencies": {
    "@prisma/client": "5.2.0",
    "prisma": "^5.2.0"
  }
}
